# Runtime dependencies (kept in sync with pyproject.toml).
# Prefer `pip install .` / `pipx install .` which installs the crawlbtc CLI.
aiohttp>=3.9
psycopg[binary]>=3.1
psycopg-pool>=3.2
python-dotenv>=1.0
structlog>=24.1
orjson>=3.9
